<resources>

    <!-- Base application theme. -->
    <style name="HomeTheme" parent="MaterialDrawerTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_drawer_primary</item>
        <item name="colorPrimaryDark">@color/material_drawer_primary_dark</item>
        <item name="colorAccent">@color/material_drawer_accent</item>

        <item name="android:colorButtonNormal">@color/primary</item>
        <item name="android:colorControlHighlight">@color/primary_dark</item>
    </style>

    <style name="LoginChooserTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/material_drawer_primary</item>
        <item name="colorPrimaryDark">@color/material_drawer_primary_dark</item>
        <item name="colorAccent">@color/material_drawer_accent</item>

        <item name="android:colorButtonNormal">@color/primary</item>
        <item name="android:colorControlHighlight">@color/primary_dark</item>
    </style>


</resources>
